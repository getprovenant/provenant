# SBOM examples

Real software bills of materials produced by [Provenant](https://github.com/getprovenant/provenant), one directory per target. Each target directory contains an SPDX tag-value document ([`sbom.spdx`](ripgrep/sbom.spdx)) and a CycloneDX JSON document ([`sbom.cdx.json`](ripgrep/sbom.cdx.json)), plus a short provenance README.

These examples are generated by Provenant 1.0.0 (the current build) scanning each project at a pinned commit with license, package, and copyright detection enabled.

## Targets

| Target | Ecosystem | Release | Pinned commit |
| ------ | --------- | ------- | ------------- |
| [`ripgrep`](ripgrep/) | Rust / Cargo | [`15.2.0`](https://github.com/BurntSushi/ripgrep/releases/tag/15.2.0) | `e89fff89ac9af12e8d4ce9d5fd07beb408ca730f` |
| [`express`](express/) | JavaScript / npm | [`v5.2.1`](https://github.com/expressjs/express/releases/tag/v5.2.1) | `dbac741a49a5a64336b70c06e85c2e2706e36336` |
| [`flask`](flask/) | Python / PyPI | [`3.1.3`](https://github.com/pallets/flask/releases/tag/3.1.3) | `22d924701a6ae2e4cd01e9a15bbaf3946094af65` |

## Verification

Every target was compared against ScanCode with the `compare-outputs` xtask before being included here. None carry a medium-or-major regression (a real missing license or copyright, garbled detections, scan errors, or materially wrong package inventory); the remaining differences are benign (ScanCode noise, unknown-license-reference placeholders, or Provenant's cleaner source-faithful rendering).

## Regeneration

These files are generated and drift-checked in CI. Regenerate them with a single command:

```bash
cargo run --manifest-path xtask/Cargo.toml --bin generate-sbom-examples
```

Verify they are current (as CI does) with:

```bash
cargo run --manifest-path xtask/Cargo.toml --bin generate-sbom-examples -- --check
```
