# Gradle multiproject

Synthetic Gradle multiproject fixture: a `settings.gradle` declaring one
included module, with nested sources owned by the module package and
immediate `build/` output left unowned.
